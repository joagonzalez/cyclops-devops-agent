User prompt: "{{ user_prompt }}"

Task: Decompose this prompt and extract potential metric names, aggregation functions, and labels.

Respond in JSON:
{
    "metric_names": [],
    "agg_func": "",
    "labels": {}
}
